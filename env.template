# MongoDB Atlas 연결 문자열
# 1. https://www.mongodb.com/cloud/atlas 가입
# 2. M0 (무료) 클러스터 생성
# 3. Database Access에서 사용자 생성
# 4. Network Access에서 0.0.0.0/0 추가
# 5. Connect → Connect your application에서 문자열 복사
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/massage-platform?retryWrites=true&w=majority

# JWT 비밀키 (아래 명령어로 생성)
# Mac/Linux: openssl rand -base64 32
# Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=생성한-랜덤-문자열-32자-이상

# NextAuth 비밀키 (위와 동일한 방법으로 생성)
NEXTAUTH_SECRET=생성한-랜덤-문자열-32자-이상

# NextAuth URL
# 로컬: http://localhost:3000
# 배포: https://your-app-name.vercel.app
NEXTAUTH_URL=http://localhost:3000

# ===== 선택사항 (실제 SMS 발송 시 필요) =====

# NAVER Cloud Platform SMS API
# https://www.ncloud.com/ 가입 후 발급
# SMS_API_KEY=ncp-iam-access-key
# SMS_API_SECRET=ncp-iam-secret-key
# SMS_SENDER_PHONE=01012345678

# KakaoTalk SMS API (대안)
# https://developers.kakao.com/ 가입 후 발급
# KAKAO_API_KEY=your-kakao-api-key
# KAKAO_SENDER_KEY=your-kakao-sender-key

# NICE 본인인증 API (대안)
# https://www.niceid.co.kr/ 가입 후 발급
# NICE_CLIENT_ID=your-nice-client-id
# NICE_CLIENT_SECRET=your-nice-client-secret

# ===== 사용 방법 =====
# 1. 이 파일을 .env.local로 복사:
#    cp env.template .env.local
# 2. 위의 값들을 실제 값으로 변경
# 3. .env.local은 절대 Git에 커밋하지 마세요!

